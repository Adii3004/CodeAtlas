fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic-settings==2.10.1
pytest==8.4.1
ruff==0.16.0
# tree-sitter pinned to 0.25.x: the 0.26.0 bindings are ABI-incompatible
# with the tree-sitter-python 0.25.0 grammar wheel (heap corruption).
tree-sitter==0.25.2
tree-sitter-python==0.25.0
networkx==3.6.1
numpy==2.5.1
# Matched to the qdrant server version in docker-compose.yml (1.15.1).
qdrant-client==1.15.1
google-genai==2.14.0
